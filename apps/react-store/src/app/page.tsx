// eslint-disable-next-line @nx/enforce-module-boundaries
import { Counter } from '../../../../libs/home/src/components';

export default function Index() {
    return (
        <Counter></Counter>
    );
}
